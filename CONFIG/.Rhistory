0.01 * (20/100)
resolution <- 50
# LOCAL FUNCTIONS ---------------------------------------------------------
cli::cli_h2("FUNCTIONS")
r_files <- list.files(path = "./R",
pattern = "*.R",
full.names = TRUE)
for (file in r_files) {
cat(paste0("Sourcing: ", basename(file), "\n"))
source(file)
}
cat("\n")
# PARAMETERS --------------------------------------------------------------
cli::cli_h2("PARAMETERS")
## Load -------------------------------------------------------------------
cat("------Load------\n\n")
source("./CONFIG/default.R")
taux <- 20/100
resolution <- 50
params$events$r_mig - (params$events$r_mig * taux)
setwd("./SIMULATEURS/WAIS_transposons")
